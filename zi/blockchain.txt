import TRANSACTION as tt

class blockchain:
    trans=[]; prev_hashb=0; Merkel=0; hashb=0;
    #t=tt.transaction()
    def __init__(self,gt):
        self.trans.append(gt)
        self.prev_hashb="genesisb"
        self.Merkel=create_merkel(self,self.trans)
        self.hashb=0

def create_merkel(self,trans):
        #for arity 2
    if(len(trans)%2 !=0):
        trans.append(trans[-1])   #duplicate the last ele to make count even